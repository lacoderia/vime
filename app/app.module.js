'use strict';

// Declare app level module which depends on views, and components
angular.module('vime', [
    'ngMaterial'
]);

angular.module('vime')
    .constant('API_URL', 'http://localhost/vime');